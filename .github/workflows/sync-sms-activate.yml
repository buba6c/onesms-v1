name: Sync SMS-Activate

on:
  schedule:
    # Toutes les 30 minutes pour des données fraîches
    - cron: '*/30 * * * *'
  workflow_dispatch: # Permet déclenchement manuel

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger sync-sms-activate Edge Function
        run: |
          response=$(curl -s -w "\n%{http_code}" -X POST \
            'https://htfqmamvmhdoixqcbbbw.supabase.co/functions/v1/sync-sms-activate' \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imh0ZnFtYW12bWhkb2l4cWNiYmJ3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM2MjQ4MjgsImV4cCI6MjA3OTIwMDgyOH0.HQ5KsI86nrDidy4XLh1OnOSpM8c1ZnY3fYo-UF5Jtyg" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imh0ZnFtYW12bWhkb2l4cWNiYmJ3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM2MjQ4MjgsImV4cCI6MjA3OTIwMDgyOH0.HQ5KsI86nrDidy4XLh1OnOSpM8c1ZnY3fYo-UF5Jtyg" \
            -H "Content-Type: application/json" \
            -d '{}')
          
          http_code=$(echo "$response" | tail -n 1)
          body=$(echo "$response" | sed '$d')
          
          echo "HTTP Status: $http_code"
          echo "Response: $body"
          
          if [ "$http_code" -ne 200 ]; then
            echo "❌ Sync SMS-Activate failed with status $http_code"
            exit 1
          fi
          
          echo "✅ SMS-Activate synchronized successfully"
      
      - name: Parse and display stats
        run: |
          echo "📊 SMS-Activate synchronized"
          echo "   - Services updated with icons and categories"
          echo "   - Pricing rules updated"
          echo "   - Service totals calculated (total_available)"
          echo "🔄 Next sync in 30 minutes"
